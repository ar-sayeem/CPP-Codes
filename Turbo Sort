#include<bits/stdc++.h>         // "bits/stdc++.h" is a header file that include all the standard libraries.
using namespace std;
int main()
{
    int t;
    cin >> t;           // User input 't' for test case.
    int a[t];
    for(int i=0; i<t; i++)
        cin >> a[i];            // user input values through array.
        sort(a,a+t);            // In-built C++ sorting function (first element, last element).
        for(int i=0; i<t; i++)
            cout << a[i] << endl;        // Output through for-loop.
    return 0;
}
