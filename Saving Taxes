#include<bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    cin>>t;
    while(t--)
    {
        int x, y;
        cin>>x>>y;
        cout<<x-y<<endl;
    }

    return 0;
}

/*
eXplanation::

>> We will take an integer number for test case 't'. In the code, we used a 'while loop'.

>> In the while loop, two integers, 'x' and 'y' are taken as input.

>> x = the amount you earned || y = the amount above which you will have to pay taxes.

>>  We just need to subtract y from x means [x-y].

>> Then we print the value of x-y.
*/
